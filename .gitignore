.venv/
__pycache__/
*.pyc
data/*.db
.env
*.eml
*.xlsx
!app/static/template/*.xlsx

# build del ayudante: carpetas de trabajo de PyInstaller, no van al repo.
# El .zip final (app/static/downloads/) SI va al repo -- lo sirve la app
# por link, tiene que viajar con el codigo a donde se despliegue.
helper/build/
helper/dist/
helper/paquete/
helper/*.spec
